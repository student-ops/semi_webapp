/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  output:'export',
}

module.exports = {
  images: {
    unoptimized: true
  }
}


// module.exports = nextConfig
